\* TLC config for the finite instance AriaInstance
\* CONSTRAINT bounds t for finiteness only; abstract Spec keeps t ∈ Nat.

SPECIFICATION Spec

INVARIANT Safety
INVARIANT Inv1
INVARIANT Inv2
INVARIANT Inv3
INVARIANT Inv4
INVARIANT TypeOK

\* Model-checking bound (does not change Aria.tla Spec)
CONSTRAINT StateConstraint

CHECK_DEADLOCK FALSE
