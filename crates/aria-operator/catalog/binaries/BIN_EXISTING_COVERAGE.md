# BIN.EXISTING_COVERAGE

**Operator:** `existing-coverage` · **layer:** HOST · **class:** HOST · **parent:** —
**Crate:** `pcvc-existing-coverage` · **verify:** True · **result:** `host.existing_coverage`

## Why

Not an AriA operator. Reads Observation Plan existingCoverage. Observe without reading app.* Neon.

## Function

Host toolkit. Not a research operator. Empty limitation, no Φ (B6).

## Use

`work --binary BIN.EXISTING_COVERAGE` or `work --json` with `ops: ["BIN.EXISTING_COVERAGE"]`

Production callback: working vertical or nothing. Forget is not delete — original payload stays in `source`.

## Declared neighborhood

- node types: —
- relationships: —
- anchor tags: EXISTING_COVERAGE, REQUIREMENT_ID
- property key: —

## Maps that consume this

- (not a primary binary on sheet 05, or is itself a map mixer)

## Sheet notes

J3 / R1 first work.

## Important

- No Trust / Use / Goal fields.
- Does not rewrite other binaries' verticals.
- Empty declared types → omitted from `aria-work-v1` results.
