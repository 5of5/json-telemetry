# BIN.NEO4J_AURA

**Operator:** `neo4j-aura` · **layer:** HOST · **class:** HOST · **parent:** —
**Crate:** `pcvc-neo4j-aura` · **verify:** True · **result:** `host.neo4j_aura`

## Why

Not an AriA operator. Checked-in Cypher templates only. Returns projection records. This is the pass-through that makes B3 possible.

## Function

Host toolkit. Not a research operator. Empty limitation, no Φ (B6).

## Use

`work --binary BIN.NEO4J_AURA` or `work --json` with `ops: ["BIN.NEO4J_AURA"]`

Production callback: working vertical or nothing. Forget is not delete — original payload stays in `source`.

## Declared neighborhood

- node types: projection
- relationships: projection
- anchor tags: CYPHER_TEMPLATE, SUBJECT_ID, TAG
- property key: —

## Maps that consume this

- (not a primary binary for any map, or is itself a map mixer)

## Sheet notes

ARE WE PULLING FROM NEO4j? YES — templates only.

## Important

- No Trust / Use / Goal fields.
- Does not rewrite other binaries' verticals.
- Empty declared types → omitted from `aria-work-v1` results.
