# BIN.PROP.VIRAL_COEFFICIENT

**Operator:** `PROP.VIRAL_COEFFICIENT` · **layer:** RESIDUAL · **class:** PROP · **parent:** PORTFOLIO
**Crate:** `aria-res-prop-viral-coefficient` · **verify:** True · **result:** `residual.prop.viral_coefficient`

## Why

Listed Binary Repository v1 identity. Independent calculation; no borrowed scores.

## Function

Property residual. Echoes one declared key when present. Never mints Trust.

## Use

`work --binary BIN.PROP.VIRAL_COEFFICIENT` or `work --json` with `ops: ["BIN.PROP.VIRAL_COEFFICIENT"]`

Production callback: working vertical or nothing. Forget is not delete — original payload stays in `source`.

## Declared neighborhood

- node types: —
- relationships: —
- anchor tags: —
- property key: viral_coefficient

## Maps that consume this

- (not a primary binary on sheet 05, or is itself a map mixer)

## Important

- No Trust / Use / Goal fields.
- Does not rewrite other binaries' verticals.
- Empty declared types → omitted from `aria-work-v1` results.
