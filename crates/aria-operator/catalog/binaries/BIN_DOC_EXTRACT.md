# BIN.DOC_EXTRACT

**Operator:** `document-extract` · **layer:** HOST · **class:** HOST · **parent:** —
**Crate:** `pcvc-document-extract` · **verify:** False · **result:** `host.document_extract`

## Why

Not an AriA operator. Allowlisted document bytes only if Product accepts the class. Marked F until Product accepts the class.

## Function

Host toolkit. Not a research operator. Empty limitation, no Φ (B6).

## Use

`work --binary BIN.DOC_EXTRACT` or `work --json` with `ops: ["BIN.DOC_EXTRACT"]`

Production callback: working vertical or nothing. Forget is not delete — original payload stays in `source`.

## Declared neighborhood

- node types: —
- relationships: —
- anchor tags: DOCUMENT_CLASS, BYTES
- property key: —

## Maps that consume this

- (not a primary binary for any map, or is itself a map mixer)

## Sheet notes

Pending Product acceptance of document class.

## Important

- No Trust / Use / Goal fields.
- Does not rewrite other binaries' verticals.
- Empty declared types → omitted from `aria-work-v1` results.
