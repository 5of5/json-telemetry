# BIN.HTTP_ALLOWLIST

**Operator:** `allowlisted-http` · **layer:** HOST · **class:** HOST · **parent:** —
**Crate:** `pcvc-http-allowlist` · **verify:** True · **result:** `host.allowlisted_http`

## Why

Not an AriA operator. Named external APIs already on the Observation Plan.

## Function

Host toolkit. Not a research operator. Empty limitation, no Φ (B6).

## Use

`work --binary BIN.HTTP_ALLOWLIST` or `work --json` with `ops: ["BIN.HTTP_ALLOWLIST"]`

Production callback: working vertical or nothing. Forget is not delete — original payload stays in `source`.

## Declared neighborhood

- node types: —
- relationships: —
- anchor tags: API_NAME, PLAN_URL
- property key: —

## Maps that consume this

- (not a primary binary for any map, or is itself a map mixer)

## Sheet notes

No arbitrary URL (A3).

## Important

- No Trust / Use / Goal fields.
- Does not rewrite other binaries' verticals.
- Empty declared types → omitted from `aria-work-v1` results.
