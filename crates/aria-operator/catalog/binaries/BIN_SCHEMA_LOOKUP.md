# BIN.SCHEMA_LOOKUP

**Operator:** `schema-lookup` · **layer:** HOST · **class:** HOST · **parent:** —
**Crate:** `pcvc-schema-lookup` · **verify:** True · **result:** `host.schema_lookup`

## Why

Not an AriA operator. Enrichable fields and allowed resultDefinitionRef values. Compile-time closed world.

## Function

Host toolkit. Not a research operator. Empty limitation, no Φ (B6).

## Use

`work --binary BIN.SCHEMA_LOOKUP` or `work --json` with `ops: ["BIN.SCHEMA_LOOKUP"]`

Production callback: working vertical or nothing. Forget is not delete — original payload stays in `source`.

## Declared neighborhood

- node types: —
- relationships: —
- anchor tags: RESULT_DEFINITION_REF, FIELD
- property key: —

## Maps that consume this

- (not a primary binary for any map, or is itself a map mixer)

## Sheet notes

S4 — no invented result types.

## Important

- No Trust / Use / Goal fields.
- Does not rewrite other binaries' verticals.
- Empty declared types → omitted from `aria-work-v1` results.
