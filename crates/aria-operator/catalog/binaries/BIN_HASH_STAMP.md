# BIN.HASH_STAMP

**Operator:** `hash-stamp` · **layer:** HOST · **class:** HOST · **parent:** —
**Crate:** `pcvc-hash-stamp` · **verify:** True · **result:** `host.hash_stamp`

## Why

Not an AriA operator. Content hash over the evidence payload, not coordinator wording (S6).

## Function

Host toolkit. Not a research operator. Empty limitation, no Φ (B6).

## Use

`work --binary BIN.HASH_STAMP` or `work --json` with `ops: ["BIN.HASH_STAMP"]`

Production callback: working vertical or nothing. Forget is not delete — original payload stays in `source`.

## Declared neighborhood

- node types: —
- relationships: —
- anchor tags: CONTENT_HASH, PAYLOAD
- property key: —

## Maps that consume this

- (not a primary binary for any map, or is itself a map mixer)

## Sheet notes

Required on every writer.

## Important

- No Trust / Use / Goal fields.
- Does not rewrite other binaries' verticals.
- Empty declared types → omitted from `aria-work-v1` results.
