# BIN.FEED_BY_NAME

**Operator:** `feed.by-name` · **layer:** HOST · **class:** HOST · **parent:** —
**Crate:** `pcvc-feed-by-name` · **verify:** True · **result:** `host.feed_by_name`

## Why

Not an AriA operator. Host tool: exact path_id from registry + plan. Default read of eligible corpus. Live pull only if plan allows.

## Function

Host toolkit. Not a research operator. Empty limitation, no Φ (B6).

## Use

`work --binary BIN.FEED_BY_NAME` or `work --json` with `ops: ["BIN.FEED_BY_NAME"]`

Production callback: working vertical or nothing. Forget is not delete — original payload stays in `source`.

## Declared neighborhood

- node types: —
- relationships: —
- anchor tags: PATH_ID, ELIGIBLE_SOURCE_CLASS
- property key: —

## Maps that consume this

- (not a primary binary for any map, or is itself a map mixer)

## Sheet notes

Mode 4 host toolkit. Not a research-operator binary.

## Important

- No Trust / Use / Goal fields.
- Does not rewrite other binaries' verticals.
- Empty declared types → omitted from `aria-work-v1` results.
