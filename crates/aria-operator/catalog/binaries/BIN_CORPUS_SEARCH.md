# BIN.CORPUS_SEARCH

**Operator:** `corpus-search` · **layer:** HOST · **class:** HOST · **parent:** —
**Crate:** `pcvc-corpus-search` · **verify:** True · **result:** `host.corpus_search`

## Why

Not an AriA operator. Search Mode 1 corpora and admin_data when eligible.

## Function

Host toolkit. Not a research operator. Empty limitation, no Φ (B6).

## Use

`work --binary BIN.CORPUS_SEARCH` or `work --json` with `ops: ["BIN.CORPUS_SEARCH"]`

Production callback: working vertical or nothing. Forget is not delete — original payload stays in `source`.

## Declared neighborhood

- node types: —
- relationships: —
- anchor tags: CORPUS, QUERY, ELIGIBLE
- property key: —

## Maps that consume this

- (not a primary binary on sheet 05, or is itself a map mixer)

## Sheet notes

Eligible reuse only (section 7).

## Important

- No Trust / Use / Goal fields.
- Does not rewrite other binaries' verticals.
- Empty declared types → omitted from `aria-work-v1` results.
