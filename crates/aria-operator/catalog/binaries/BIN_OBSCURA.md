# BIN.OBSCURA

**Operator:** `obscura` · **layer:** HOST · **class:** HOST · **parent:** —
**Crate:** `pcvc-obscura` · **verify:** True · **result:** `host.obscura`

## Why

Not an AriA operator. Mode 2 headless gateway. Allowlisted hosts. No product credentials. Live only after Observe gap.

## Function

Host toolkit. Not a research operator. Empty limitation, no Φ (B6).

## Use

`work --binary BIN.OBSCURA` or `work --json` with `ops: ["BIN.OBSCURA"]`

Production callback: working vertical or nothing. Forget is not delete — original payload stays in `source`.

## Declared neighborhood

- node types: —
- relationships: —
- anchor tags: URL, ALLOWLIST, DEPTH
- property key: —

## Maps that consume this

- (not a primary binary for any map, or is itself a map mixer)

## Sheet notes

Mandatory live-fetch gateway.

## Important

- No Trust / Use / Goal fields.
- Does not rewrite other binaries' verticals.
- Empty declared types → omitted from `aria-work-v1` results.
