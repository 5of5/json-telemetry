# BIN.PROP.DECAY_HALF_LIFE

**Operator:** `PROP.DECAY_HALF_LIFE` · **layer:** RESIDUAL · **class:** PROP · **parent:** SIGNAL
**Crate:** `aria-res-prop-decay-half-life` · **verify:** True · **result:** `residual.prop.decay_half_life`

## Why

Listed catalog identity. Independent calculation; no borrowed scores.

## Function

Property residual. Echoes one declared key when present. Never mints Trust.

## Use

`work --binary BIN.PROP.DECAY_HALF_LIFE` or `work --json` with `ops: ["BIN.PROP.DECAY_HALF_LIFE"]`

Production callback: working vertical or nothing. Forget is not delete — original payload stays in `source`.

## Declared neighborhood

- node types: —
- relationships: —
- anchor tags: —
- property key: decay_half_life

## Maps that consume this

- (not a primary binary for any map, or is itself a map mixer)

## Important

- No Trust / Use / Goal fields.
- Does not rewrite other binaries' verticals.
- Empty declared types → omitted from `aria-work-v1` results.
